#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int found =0;
    int target;
    scanf("%d",&target);
    
    for(int i=0;i<n;i++){
        if(arr[i]==target){
            found =1;
        }
    }
    
    if(found==1){
        printf("Found");
    }
    else{
        printf("Not Found");
    }
    return 0;
}
