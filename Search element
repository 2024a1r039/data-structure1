#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int size,found=0;
    scanf("%d",&size);
    int list[size];
    for(int i=0;i<size;i++){
        scanf("%d",&list[i]);
    }
    int target;
    scanf("%d",&target);
    
    for(int i=0;i<size;i++){
        if(target==list[i]){
            found=1;
            break;
        }
    }
    if(found==1){
        printf("Found");
    }
    else{
        printf("Not Found");
    }
    return 0;
}
