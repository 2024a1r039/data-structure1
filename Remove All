#include <stdio.h>  

int main() {  
    int n, val, i, k = 0;    
    scanf("%d", &n);  

    int arr[100];     
    for (i = 0; i < n; i++) {  
        scanf("%d", &arr[i]);  
    }  
 
    scanf("%d", &val);  
  
    for (i = 0; i < n; i++) {  
        if (arr[i] != val) {  
            arr[k++] = arr[i]; 
        }  
    }  
    for (i = 0; i < k; i++) {  
        printf("%d ", arr[i]);  
    }  

    printf("\n");  
    return 0;  
}
