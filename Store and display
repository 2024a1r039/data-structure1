#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int size;
    scanf("%d",&size);
    int list[size];
    for(int i=0;i<size;i++){
        scanf("%d",&list[i]);
    }
    for(int i=0;i<size;i++){
        printf("%d ",list[i]);
    }
    return 0;
}
